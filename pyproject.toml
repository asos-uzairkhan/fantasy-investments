[project]
name = "fantasy-investments-data-gen"
version = "1.0.0"
description = "Generate historical stock market data CSV files for Fantasy Investments"
readme = "DATA_GENERATION.md"
requires-python = ">=3.9"
dependencies = [
    "yfinance>=0.2.0",
    "pandas>=1.3.0",
]
